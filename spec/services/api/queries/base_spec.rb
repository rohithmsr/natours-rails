RSpec.describe Api::Queries::Base do
  # TODO. We should be testing pagination, filtering, count etc

  # not that the class semi-implements functionality that makes
  # it feel like an abstract class. - C0
end
